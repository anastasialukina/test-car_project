<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('car_models', function (Blueprint $table) {
            $table->id()->comment('Identifier');

            $table->bigInteger('brand_id')->unsigned()->comment('Brand ID');
            $table->foreign('brand_id')->references('id')->on('car_brands')->restrictOnDelete();

            $table->string('name', 100)->index()->comment('Name of the car model');

            $table->timestamps();
            $table->softDeletes();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('car_models');
    }
};
